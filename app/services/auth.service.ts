import { Injectable } from '@angular/core';
import 'firebase/auth';
import firebase from '@firebase/app'

@Injectable({
  providedIn: 'root'
})
export class AuthService {
  // email: any;
  // password:any;
  constructor() { }
  //: Promise<firebase.auth.UserCredential> : Promise < any > : Promise < void> 
  
}
